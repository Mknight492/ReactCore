import React from "react";
import styles from "./failureModal.container.scss";

const failureModal = props => {
  return (
    <div className={styles.container}>
      <h2>failureModal</h2>
    </div>
  );
};

export default failureModal;
